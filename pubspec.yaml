name: camera_capture
description: A new Flutter package project.
version: 0.0.2
homepage: https://github.com/tedcrimson/camera_capture

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  camera: ^0.8.1
  gallery_previewer: ^0.0.2+1

    # path: ../gallery_previewer
    # git:
    #   url: git@github.com:tedcrimson/packages.git
    #   path: gallery_previewer
